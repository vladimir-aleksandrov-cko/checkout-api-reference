###
# Warning: this file was auto generated from OpenAPI specs using 'npm run sync-generated-specs'. Do not manually edit. 
###
type: object
description: 'Ideal Source'
allOf:
  -
    $ref: '#/components/schemas/PaymentRequestSource'
  -
    type: object
    required:
      - description
      - bic
    properties:
      description:
        maxLength: 35
        type: string
        description: A description of the order (e.g., an order reference number). Do not use HTML tags or any other such code.
      bic:
        maxLength: 11
        type: string
        description: The Bank Identifier Code (8 or 11-digits). Also known as the issuerID.
      language:
        maxLength: 2
        type: string
        description: The customer's preferred language (2-letter ISO code, e.g., `"nl"` – Dutch or `"en"` – English). If an unsupported language is entered, the standard language of the issuer is used.
